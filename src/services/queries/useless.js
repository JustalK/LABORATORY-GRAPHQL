/**
* The queries useless
* @module queries/useless
*/
'use strict'

/**
* Manage the queries for the question model
**/
module.exports = {
  /**
  * Query useless
  * @param {boolean} Return true
  **/
  useless: async (_, args) => {
    return true
  }
}
